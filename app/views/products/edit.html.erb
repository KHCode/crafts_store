<div class="container mt-4">
  <h1>Edit Product</h1>
  
  <div class="card">
    <div class="card-body">
      <%= render 'form', product: @product %>
    </div>
  </div>
  
  <div class="mt-3">
    <%= link_to 'View Product', @product, class: 'btn btn-outline-primary me-2' %>
    <%= link_to 'Back to Products', products_path, class: 'btn btn-outline-secondary' %>
  </div>
</div>
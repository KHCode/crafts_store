<div class="container mt-4">
  <h1>Edit Your Shop</h1>
  
  <div class="card">
    <div class="card-body">
      <%= render 'form', shop: @shop %>
    </div>
  </div>
  
  <div class="mt-3">
    <%= link_to 'View Shop', @shop, class: 'btn btn-outline-primary me-2' %>
    <%= link_to 'Back to Shops', shops_path, class: 'btn btn-outline-secondary' %>
  </div>
</div>